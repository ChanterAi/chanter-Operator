{
  "//": "Static coverage for the CHANTER OS operational proof tools. These files live outside the backend program (which compiles and emits only src/), so without this config nothing typechecks them and they rot silently. Compiler options mirror apps/backend/tsconfig.json exactly, minus emit: same target, module system, and strictness, so a tool and the production code it drives are held to one standard.",
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "types": ["node"],
    "noEmit": true
  },
  "include": [
    "tools/phase2c",
    "tools/os-assembly",
    "tools/os-platform-recovery",
    "tools/os-recovery",
    "tools/os-unified",
    "tools/validation",
    "tools/persisted-approval-authority",
    "tools/platform-canonical",
    "tools/resilience-evidence"
  ],
  "//exclusion": "tools/safecommit-closeout is deliberately NOT covered. Its integration test imports five untyped .mjs modules from ../../../../tools/safecommit/, which git confirms is outside this repository. Typing it would require declarations in another repository, or allowJs over a foreign product, or implicit any — none of which are acceptable. See CHANTER_OPERATOR_CRITICAL_TEST_AND_AUTHORITY_TOOL_STATIC_COVERAGE_P0_RESULT_V1.md."
}
